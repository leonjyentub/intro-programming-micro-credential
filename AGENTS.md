# AGENTS.md

本文件是本專案中 AI 助理與自動化代理的工作指引。目標是讓後續教材擴充維持一致、可信、可教、可執行與可回溯。除非使用者另有明確要求，所有修改都應遵循本文件。

## 1. 專案定位

- 專案名稱：基礎程式設計微學分課程。
- 對象：國立臺北商業大學資訊管理系四技與二技新生，包含沒有程式經驗的學習者。
- 核心目標：讓學生看懂修課與能力地圖，建立拆解問題、實作、測試、除錯、版本管理及安全使用 AI 的基本循環。
- 教學語言：以繁體中文為主；必要的英文術語第一次出現時，應提供中文說明或中英對照。
- 教學立場：程式設計不是背誦語法；工具不是學習目標；能理解、執行、驗證、解釋與回溯，才算完成。

## 2. 重要檔案與資料夾

- `基礎程式設計微學分_程式設計學習地圖.md`：Marp 教材的主要來源與內容權威版本。
- `基礎程式設計微學分_程式設計學習地圖.pdf`：供授課與閱讀的輸出版本，應與 Marp 原始檔同步。
- `README.md`：專案入口、內容摘要與教材導覽。
- `assets/`：流程圖、架構圖、圖示與圖片。
- `assets/icons/`：產品或技術圖示；新增前應確認來源與使用方式。
- `課程科目表/`：課程地圖與修課敘述的主要依據。

若新增可執行範例，優先採用下列結構：

```text
examples/
├── java/<主題>/
├── python/<主題>/
└── README.md
```

每個範例應小而完整，附上用途、環境、執行方式、預期結果與測試資料。若新增主要資料夾或改變教材結構，必須同步更新根目錄 `README.md`。

## 3. 現有教材主線

擴充內容時，先判斷最適合放入哪一段，避免重複或打亂學習順序：

1. 資訊管理與修課地圖。
2. 資訊系統概念。
3. 程式語言與共同基礎。
4. 框架、程式生態系與應用。
5. 資料庫。
6. 專題實作與作品累積。
7. 作業系統、檔案與終端機操作。
8. Git 與 GitHub 版本管理。
9. Java、Python 與 VS Code 實務。
10. DOMjudge 評量與驗證。
11. AI 協同學習、安全與學術誠信。
12. 官方文件與延伸資源。

新增內容應能回答至少一個問題：「學生為什麼需要知道？它和前後能力如何連接？學生能做出或驗證什麼？」若無法回答，不應只為增加頁數而加入。

## 4. 編寫原則

- 先說明情境與問題，再介紹名詞、工具或語法。
- 使用新生能理解的短句、具體例子與可觀察結果，避免只有定義或大量術語。
- 保留「需求 → 步驟 → 實作 → 測試 → 修正 → 紀錄」的學習循環。
- 優先使用校園活動、成績、待辦事項、報名、候補、記帳等低門檻情境。
- 清楚區分概念、建議、課程規定與產品功能，不把選配工具寫成所有學生都必須安裝。
- 不誇大 AI、框架或單一語言的能力；不暗示使用某工具就能取代基本功。
- 不以「很簡單」「顯然」等語氣降低新手提問的正當性。
- 課名、學制、必選修與學分等內容，必須依 `課程科目表/` 或系上最新正式資料，不憑印象推測。
- 工具介面、版本、價格、方案與功能容易變動；撰寫前應查閱當下的官方文件，並避免寫成永久不變的事實。

## 5. 來源與查證

- 優先使用第一方來源：學校正式資料、產品官方文件、語言或框架官方文件、標準文件。
- 不以搜尋摘要、未查證的教學文章或 AI 回答作為關鍵技術事實的唯一依據。
- 外部資料形成的非一般性主張、版本資訊、操作方式或比較，應在對應投影片後方的 HTML 註解中加入 `[Sources]`。
- 延續現有格式：

```markdown
<!--
講稿：
補充說明與授課提示。

[Sources]
- https://example.com/official-documentation
-->
```

- 若只有來源而沒有講稿，可只保留 `[Sources]` 區塊。
- 圖片、截圖、圖示與外部素材也要留下來源或「使用者提供」註記。
- 無法確認的資訊應標示限制或暫不加入，不得自行補造版本、數字、功能、引用或操作結果。

## 6. Marp 投影片規範

- 保留原始檔的 YAML front matter、全域 CSS、頁首頁尾、頁碼與既有色彩系統。
- 不任意移除 `---` 分頁符號、`<!-- _class: ... -->` 指令、講稿或 `[Sources]` 註解。
- 沿用現有版型語意：`lead` 用於章節或收束頁；`activity` 用於學生活動；`table-medium`、`table-compact`、`git-compact` 等只在內容密度需要時使用。
- 每張投影片只承擔一個主要教學任務。內容過多時，優先拆頁或刪減，不以縮小字體硬塞。
- 標題要能單獨說明本頁重點；避免只有「介紹」「補充」等無法辨識內容的標題。
- 表格應有明確比較維度，欄數與文字量需適合 16:9 畫面；不要把段落文章塞進儲存格。
- 程式碼須保持可讀字級，單張只呈現理解當下概念所需的最小片段。
- 圖片使用相對路徑並提供有意義的 `alt` 文字；不可使用只寫「圖片」或檔名的替代文字。
- 所有學生看得見的文字應是可直接授課的成品，不得留下 TODO、提示詞、AI 工作筆記或未解決占位符。
- 修改章節順序、頁數或內容範圍後，同步更新開頭的學習地圖、章節標示與結尾回顧。

## 7. 程式範例規範

- 範例必須可以實際執行，不得只提供看似合理但未驗證的程式碼。
- 優先採用課程主線中的 Java 與 Python；新增其他語言前，先說明其教學必要性。
- 每個範例聚焦一至兩個概念，命名要清楚，避免為展示技巧而使用新生尚未學過的抽象寫法。
- 明確列出輸入、處理、輸出、限制、正常測資與至少一個邊界情況。
- 指令要標示適用平台或 shell；Windows CMD、PowerShell、macOS 與 Linux 指令不可混寫成看似通用。
- 不把帳密、API key、個資或正式資料庫連線寫進範例；只使用假資料與最小權限測試環境。
- 不示範來源不明的複製貼上命令、破壞性刪除、關閉安全機制或全域開放權限。
- 若範例需要額外套件，說明用途、版本條件與安裝方式；不要為小範例引入不必要依賴。
- 程式輸出、錯誤訊息與畫面描述必須來自實際執行；沒有執行就不可宣稱成功。

## 8. 截圖、圖示與視覺素材

- 截圖前清除姓名、帳號、Email、token、路徑中的私人資訊、瀏覽器分頁與通知。
- 優先截取完成教學所需的最小畫面範圍，確保文字在投影片上仍可辨識。
- 截圖內容若容易隨版本改變，應在講稿或圖說標示平台與必要的版本／日期背景。
- 新截圖放在 `assets/screenshots/`；檔名使用小寫英文、連字號及明確步驟，例如 `vscode-select-python-interpreter.png`。
- 外部品牌圖示優先取自官方品牌資源；不要自行改色、拉伸或誤用商標。
- 不使用裝飾性圖片填滿版面。每張圖都應協助理解流程、介面、差異或結果。
- 壓縮圖片時保留可讀性；不得用低解析度圖放大，也不得改變長寬比造成變形。

## 9. 流程圖與架構說明

- 優先使用 SVG，放在 `assets/`，並沿用教材的藍、橙、黃及中性色系。
- 一張圖只表達一個主要關係：流程、層次、資料流、責任分工或驗證循環。
- 箭頭方向、開始與結束、輸入與輸出必須明確；不要讓線穿過文字或讓讀者猜測順序。
- 架構圖應從使用者任務或真實需求開始，再連到介面、規則、資料、基礎設施與回饋。
- 清楚區分「概念架構」與「實際部署」，不可把示意圖說成已完成或已驗證的系統。
- 圖中的術語應與正文一致，並提供完整 `alt` 文字；必要時在講稿補充逐步解說。
- 新圖加入後要在實際投影片尺寸檢查，不以單獨開啟 SVG 看起來正常作為驗收。

## 10. 修改工作流程

開始前：

1. 閱讀 `README.md`、本文件及 Marp 原始檔相關章節。
2. 用檔案搜尋確認是否已有相同內容、圖像、範例或來源，避免重複建立。
3. 檢查 `git status`，保留使用者既有與無關的修改。
4. 說明預計修改的範圍、依據與驗收方式。

編輯時：

1. 只修改完成任務所需的檔案，不順手重寫無關章節。
2. 先完成內容與來源，再調整版面；不要為了版面犧牲準確性。
3. 新增素材時使用清楚、穩定的檔名，並立即更新引用與來源註記。
4. 若發現來源矛盾、工具行為改版或環境無法驗證，保留差異並明確回報。

完成前：

1. 檢查章節銜接、名詞一致、繁體中文、連結、圖片路徑、程式碼與來源。
2. 對所有新增或修改的程式範例執行實測，記錄實際命令與結果。
3. 將 Marp 原始檔輸出至暫存 PDF，檢查所有頁面後，才更新專案中的正式 PDF。
4. 若本機沒有既有 Marp 或 PDF 工具，不得擅自安裝或假稱已完成渲染；應回報尚未執行的驗證。
5. 執行 `git diff --check`，再檢查 `git diff` 與 `git status`，確認沒有秘密資訊、暫存檔或無關變更。

## 11. 視覺與輸出驗收

投影片或 PDF 有變更時，必須進行完整頁面檢查，而不只檢查文字或頁數：

- 每一頁都要渲染並檢視。
- 不得有文字溢出、截斷、重疊、意外換行或字級過小。
- 圖片、圖示、程式碼及表格不得超出版面，內容必須清晰可讀。
- 頁首、頁尾、頁碼、章節順序與導覽資訊保持一致。
- 相對路徑圖片必須在 Marp 與 PDF 中都能正常顯示。
- PDF 的文字抽取可作為內容檢查，但不能取代逐頁視覺檢查。
- 若新增頁面，確認前後頁的教學節奏合理，並評估是否需要更新 README 摘要。

## 12. Git 與安全界線

- 修改前後都查看 `git status`；不要覆蓋、刪除或還原不屬於本次任務的變更。
- 不使用 `git reset --hard`、`git checkout --` 或大範圍刪除來處理工作區。
- 除非使用者明確要求，不要 commit、push、建立分支、建立 Pull Request 或改寫歷史。
- 不將 `.env`、帳密、token、個資、正式資料、編輯器暫存與建置產物加入 Git。
- 更改遠端、依賴、建置方式或檔案結構前，先確認是否超出使用者要求。

## 13. 完成回報

回覆使用者時應簡要列出：

- 新增或修改了哪些檔案與內容。
- 內容依據及重要設計取捨。
- 實際完成的程式測試、Marp／PDF 渲染與逐頁檢查。
- 尚未驗證、需要人工決定或可能隨版本變動的部分。
- 是否仍有未提交變更。

不可只說「已完成」；完成狀態必須能由檔案、測試、渲染結果或 Git 差異加以確認。
